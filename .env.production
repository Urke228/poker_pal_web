# Production build target: the deployed Cloud Functions API.
# Used by `vite build` (production mode); overrides .env.local per Vite precedence.
VITE_API_BASE_URL=https://us-central1-pokerpal-a1451.cloudfunctions.net/api
